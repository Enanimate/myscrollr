services:
  mock-yahoo:
    build:
      context: ./yahoo
      dockerfile: Dockerfile
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9005/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
