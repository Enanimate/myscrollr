services:
  mock-apisports:
    build:
      context: ./apisports
      dockerfile: Dockerfile
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9004/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
