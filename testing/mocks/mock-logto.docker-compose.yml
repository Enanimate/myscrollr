services:
  mock-logto:
    build:
      context: .
      dockerfile: Dockerfile
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9001/oidc/jwks || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
